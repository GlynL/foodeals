{
  "name": "foodeals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A curated food-deals aggregator (learning project for the OpenSpec workflow).",
  "bin": {
    "foodeals": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "cli": "node dist/cli/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
